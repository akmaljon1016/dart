import 'package:flutter/cupertino.dart';

class SmoothieTab extends StatelessWidget {
  const SmoothieTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("SmoothieTab tabs"),),
    );
  }
}
