import 'package:flutter/cupertino.dart';

class PancakeTab extends StatelessWidget {
  const PancakeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Pancake tabs"),),
    );
  }
}
